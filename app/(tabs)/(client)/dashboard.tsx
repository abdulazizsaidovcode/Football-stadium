import React from 'react'
import ClientDashboard from '@/app/(pages)/(client)/(dashboard)/dashboard'

const ClientTabDashboard = () => {
  return <ClientDashboard />
}

export default ClientTabDashboard;